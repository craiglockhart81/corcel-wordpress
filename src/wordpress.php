<?php

if (! function_exists('apply_filters')) {
    /**
     * Add an element to an array using "dot" notation if it doesn't exist.
     *
     * @param  array  $array
     * @param  string  $key
     * @param  mixed  $value
     * @return array
     */
    function apply_filters($array, $key, $value)
    {
    }
}
